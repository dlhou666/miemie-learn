# 咩咩学 · 换一台电脑继续开发

仓库：https://github.com/dlhou666/miemie-learn
线上：https://dlhou666.github.io/miemie-learn/

---

## 一、新机器首次准备（约 5 分钟）

### 1. 装 Git

Windows：https://git-scm.com/download/win （一路 Next 即可）
macOS：`brew install git`，或装 Xcode Command Line Tools

验证：`git --version`

### 2. 克隆仓库

```bash
git clone https://github.com/dlhou666/miemie-learn.git
cd miemie-learn
```

### 3. 首次推送授权

GitHub 已不支持账号密码登录。第一次 `git push` 会弹浏览器窗口，点 Authorize 即可（Windows 自带 Git Credential Manager，授权一次后长期记住）。

若弹窗要求填密码 → 去 https://github.com/settings/tokens/new 生成一个 classic token（勾 `repo`），把那串字符当密码填。

---

## 二、本地预览

项目是纯静态页面，但**PWA 的 Service Worker 只在 https 或 localhost 下注册**，所以别直接双击 `index.html`（那样虽有界面，但离线缓存、添加到主屏幕等功能失效）。

用本地服务器打开：

```bash
# Python（几乎都自带）
python -m http.server 8000

# 或 Node
npx serve .
```

浏览器访问 `http://localhost:8000` 即可。

---

## 三、改代码后提交

```bash
git add -A
git commit -m "说明改了什么"
git push
```

推送到 main 后，GitHub Pages 会在**约 1 分钟内**自动重新部署。

> ⚠️ **改了 css / js / 图片，务必把 `sw.js` 里的 `CACHE` 版本号 +1**（例如 `miemie-v3` → `miemie-v4`）。
> 否则 iPad 上已添加到主屏幕的 App 会一直读旧缓存，看不到新界面 —— 这是最容易踩的坑。

---

## 四、如果 `git push` 失败（代理环境）

部分网络环境下 `git push` 会报：

```
CONNECT tunnel failed, response 502
```

这是代理拦了 `github.com`，但 `api.github.com` 通常是通的。此时改用仓库自带的脚本：

```bash
node tools/gh-api-push.js <你的令牌> dlhou666 miemie-learn
```

脚本会把当前 git 索引里的全部内容通过 API 提交。需要先 `git add -A` 把改动加进索引。

**令牌权限**：勾 `repo` 够用；若要提交 `.github/workflows/` 下的文件，还必须勾 **`workflow`**，否则 GitHub 会返回 404（它对无权限的 workflow 文件故意返回 404 而不是 403，很容易误判）。

---

## 五、在 WorkBuddy / AI 助手里继续

把克隆下来的项目目录作为**工作目录**打开，让助手读取该目录即可接着改。建议开场说明：

> 这是「咩咩学」项目，目录在 xxx，GitHub 仓库是 dlhou666/miemie-learn。请先读 `README.md` 和 `GITHUB部署步骤.md` 了解结构。

关键文件：

| 文件 | 作用 |
|---|---|
| `index.html` | 页面骨架 |
| `css/app.css` | 全部样式（含 6 套主题变量、iPhone/iPad 响应式） |
| `js/data.js` | 预设数据：分类、难度、勋章、奖励、主题 |
| `js/store.js` | 持久化、多账户、打卡/兑换/勋章结算、备份导入导出 |
| `js/app.js` | 视图渲染与交互 |
| `sw.js` | 离线缓存（**改样式记得改这里的 CACHE 版本号**） |
| `ios/` | iOS 原生壳工程（含 `MieMieLearn/Web/` 同步副本） |

---

## 六、几个约定的注意事项

1. **改了 H5 要同步到 iOS 工程**：改动 css/js/index.html 后，把文件同步到 `ios/MieMieLearn/Web/` 对应目录，否则原生壳里还是旧版本。

2. **数据不在仓库里**：孩子的打卡记录、小红花都存在 iPad 本地浏览器存储，GitHub 上只有代码。换设备靠 App 内的「导出 JSON / 导入」迁移。

3. **iOS 编译需要 workflow 文件**：`.github/workflows/ios-build.yml` 目前尚未提交到仓库（首次部署的令牌缺 `workflow` 权限）。要启用云端编译，需要补上这个文件。

4. **旧机器上的本地副本**：如果之前那台电脑的 `mie-mie-app` 是用 API 方式提交的，它的 git 历史与远程**不共享祖先**。若要在那台机器上继续用 git，建议重新 `git clone` 一份，不要在原目录执行 `git pull`（会报 unrelated histories）。
